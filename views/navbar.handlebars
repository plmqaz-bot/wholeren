<header id="global-header" class="navbar">
    <a class="ghost-logo" href="{{admin_url absolute="true" frontend="true"}}" data-off-canvas="left" title="{{admin_url absolute="true" frontend="true"}}">
        <span class="hidden" id="rank">{{#if currentUser.rank}}{{currentUser.rank}}{{/if}} </span>
    </a>
    <nav id="global-nav" role="navigation">
        <ul id="main-menu" >
            {{#each adminNav}}
                {{#if display}}
                    <li class="{{navClass}}{{#if selected}} active{{/if}}"><a href="{{admin_url}}{{path}}">{{name}}</a></li>
                {{/if}}
            {{/each}}

            <li id="usermenu" class="usermenu subnav">
                <a href="#" data-toggle="ul" class="dropdown">
                    <img class="avatar" src="{{#if currentUser.image}}{{currentUser.image}}{{else}}{{asset "img/user-image.png"}}{{/if}}" alt="Avatar" />
                    <span class="name">{{#if currentUser.nickname}}{{currentUser.nickname}}{{else}}{{currentUser.email}}{{/if}}</span>
                </a>
                <ul class="overlay">
                    <li class="usermenu-profile"><a href="{{admin_url}}/settings/user/">Your Profile</a></li>
                    <li class="divider"></li>
                    <li class="usermenu-help"><a href="">Help / Support</a></li>
                    <li class="divider"></li>
                    <li class="usermenu-signout"><a href="{{admin_url}}/signout/">Sign Out</a></li>
                </ul>
            </li>
        </ul>
    </nav>
</header>
